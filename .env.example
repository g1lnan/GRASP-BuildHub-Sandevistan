# Database
DATABASE_URL=

# Groq — active AI provider for the prototype (gpt-oss models)
GROQ_API_KEY=

# Anthropic / Claude — kept for the calibration/production path (DESIGN §6.2)
ANTHROPIC_API_KEY=

# ASR Provider
ASR_PROVIDER=groq
ASR_API_KEY=

# Object Storage — must be a Vietnam region (NFR-6)
S3_ENDPOINT=
S3_REGION=ap-southeast-1
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET=

# Auth.js
AUTH_SECRET=

# Jobs worker — shared secret required to call POST /api/jobs/tick (cron only)
JOBS_TICK_SECRET=

# Cost conversion (VND per USD)
FX_VND_PER_USD=26000

# Audio retention
AUDIO_RETENTION_HOURS=24
